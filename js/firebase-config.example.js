// Copy this file to js/firebase-config.js and replace the placeholder values with your Firebase project config.
// Then enable Firestore in your Firebase console (in test mode or with appropriate rules).

/* Example:
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef123456"
};

// After creating js/firebase-config.js, the blog will automatically save posts to Firestore
*/

// Minimal placeholder so the script won't error if left absent
const firebaseConfig = null;
